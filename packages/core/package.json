{
  "name": "mcp-server-caldav",
  "description": "A Model Context Protocol (MCP) server enabling secure access to CalDAV APIs",
  "license": "Apache-2.0",
  "version": "0.1.0",
  "type": "module",
  "author": {
    "name": "Óscar Carrasco",
    "email": "oscar@railmap.systems",
    "url": "https://railmap.systems"
  },
  "scripts": {
    "dev": "cross-env NODE_OPTIONS='--import tsx' NODE_ENV=development rspack serve",
    "build": "cross-env NODE_OPTIONS='--import tsx' NODE_ENV=production rspack build",
    "test": "cross-env NODE_OPTIONS='--import tsx' node --test",
    "test:watch": "cross-env NODE_OPTIONS='--import tsx' node --test --watch",
    "test:unit": "cross-env NODE_OPTIONS='--import tsx' node --test src/__tests__/*.test.ts",
    "test:integration": "cross-env NODE_OPTIONS='--import tsx' node --test src/__tests__/integration/**/*.test.ts",
    "check": "biome check --write",
    "format": "biome format --write"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "tsdav": "^2.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@rspack/cli": "catalog:",
    "@rspack/core": "catalog:",
    "cross-env": "^7.0.3",
    "supports-color": "^8.1.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
